import * as React from 'react';
import { Text, View, StyleSheet } from 'react-native';

export const MyContributionsTabScreen = () => {
  return (
    <View style={styles.container}>
      <Text>MyContributionsTabScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
