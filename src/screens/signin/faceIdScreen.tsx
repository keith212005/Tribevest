import * as React from 'react';
import { Text, View, StyleSheet } from 'react-native';

export const FaceIdScreen = () => {
  return (
    <View style={styles.container}>
      <Text>FaceIdScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});
